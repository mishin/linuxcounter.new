{% extends "SywFrontMainBundle::base.html.twig" %}

{% block body %}

    <!-- Page Content -->
    <div class="container page">
        <div class="row">
            <div class="col-sm-12">
                <div class="panel panel-default">
                    <div class="panel-heading">
                        <h3 class="panel-title">{{ title }}</h3>
                    </div>
                    <div class="panel-body">
                        <p>Actually there are {{ stats.usernum }} users with {{ stats.machinenum }} machines registered in our database.</p>
                        <p>The city with the most registered users is {{ stats.mostcity }} in {{ stats.citycountry }} ({{ stats.cityusernum }} users).</p>


                    </div>
                </div>
            </div>
        </div>
    </div>

{% endblock %}
